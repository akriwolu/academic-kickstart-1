[build]
  publish = "public"
  command = "npm install && hugo mod tidy && hugo"

[build.environment]
  HUGO_VERSION = "0.153.0"
  GO_VERSION = "1.22.1"
  NODE_VERSION = "18"
  TZ = "Europe/Berlin"

[context.production.environment]
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
